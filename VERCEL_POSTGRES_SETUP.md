# Настройка Vercel Postgres для CRM системы

Это руководство поможет вам настроить Vercel Postgres для вашего проекта.

## Зачем нужен Vercel Postgres?

SQLite не работает на Vercel (serverless environment), поэтому для production нужна облачная база данных. Vercel Postgres - это полностью управляемая PostgreSQL база данных, оптимизированная для Vercel.

## Шаг 1: Создание Vercel Postgres Database

### 1.1 В Vercel Dashboard

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Перейдите в раздел **Storage** в боковом меню
3. Нажмите **Create Database**
4. Выберите **Postgres**
5. Введите имя базы данных (например, `rent-car-crm-db`)
6. Выберите регион (ближайший к вашим пользователям)
7. Нажмите **Create**

### 1.2 Подключение к проекту

1. После создания БД, нажмите **Connect Project**
2. Выберите ваш проект `rent-car-crm`
3. Нажмите **Connect**

Vercel автоматически добавит переменные окружения в ваш проект:
- `POSTGRES_URL` - Connection string для приложения
- `POSTGRES_PRISMA_URL` - Connection string для Prisma
- `POSTGRES_URL_NON_POOLING` - Direct connection (без пулинга)
- `POSTGRES_USER` - Имя пользователя
- `POSTGRES_HOST` - Хост базы данных
- `POSTGRES_PASSWORD` - Пароль
- `POSTGRES_DATABASE` - Имя базы данных

## Шаг 2: Настройка переменных окружения

### 2.1 Обязательные переменные в Vercel

Перейдите в **Settings → Environment Variables** вашего проекта и добавьте:

```
NODE_ENV=production
JWT_SECRET=ваш_очень_секретный_ключ_минимум_32_символа
PORT=5000
```

**ВАЖНО:** `POSTGRES_URL` уже автоматически установлена после подключения базы данных.

### 2.2 Генерация надежного JWT_SECRET

Используйте одну из команд:

**Node.js:**
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

**OpenSSL:**
```bash
openssl rand -hex 32
```

**PowerShell:**
```powershell
-join ((65..90) + (97..122) + (48..57) | Get-Random -Count 32 | ForEach-Object {[char]$_})
```

## Шаг 3: Инициализация базы данных

### 3.1 Автоматическая инициализация

База данных автоматически инициализируется при первом деплое. Таблицы создаются с помощью скрипта [server/database/db-postgres.js](server/database/db-postgres.js).

### 3.2 Проверка таблиц (опционально)

Если хотите проверить, что таблицы созданы:

1. В Vercel Dashboard → Storage → ваша БД
2. Перейдите на вкладку **Data**
3. Или используйте вкладку **Query** для выполнения SQL

Выполните:
```sql
SELECT table_name
FROM information_schema.tables
WHERE table_schema = 'public';
```

Вы должны увидеть:
- users
- customers
- vehicles
- bookings
- payments
- maintenance

## Шаг 4: Деплой приложения

### 4.1 Через Git (рекомендуется)

```bash
git add .
git commit -m "Configure Vercel Postgres"
git push
```

Vercel автоматически запустит новый деплой.

### 4.2 Через Vercel CLI

```bash
vercel --prod
```

## Шаг 5: Проверка работы

После деплоя проверьте:

### 5.1 API Health Check
```
https://your-app.vercel.app/api/health
```

Должен вернуть:
```json
{
  "status": "OK",
  "message": "Rent Car CRM API is running"
}
```

### 5.2 Создание первого пользователя

Используйте API endpoint для регистрации:

```bash
curl -X POST https://your-app.vercel.app/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "email": "admin@rentcar.com",
    "password": "Admin123!",
    "role": "admin"
  }'
```

### 5.3 Проверка логина

Откройте `https://your-app.vercel.app/` и войдите с:
- Email: `admin@rentcar.com`
- Password: `Admin123!`

## Архитектура базы данных

### Таблицы

1. **users** - Пользователи системы
   - Аутентификация и авторизация
   - Роли: admin, employee

2. **customers** - Клиенты
   - Контактная информация
   - Водительское удостоверение
   - История бронирований

3. **vehicles** - Автомобили
   - Детали транспортного средства
   - Статус (available, rented, maintenance)
   - Тарифы аренды

4. **bookings** - Бронирования
   - Связь клиента и автомобиля
   - Даты аренды
   - Статусы и платежи

5. **payments** - Платежи
   - История транзакций
   - Методы оплаты
   - Привязка к бронированиям

6. **maintenance** - Обслуживание
   - История ремонтов
   - Плановое ТО

## Локальная разработка с PostgreSQL

Если хотите использовать PostgreSQL локально:

### 1. Установите PostgreSQL

**Windows:**
```powershell
winget install PostgreSQL.PostgreSQL
```

**Mac:**
```bash
brew install postgresql
```

**Linux:**
```bash
sudo apt-get install postgresql
```

### 2. Создайте локальную БД

```sql
createdb rentcar_dev
```

### 3. Установите POSTGRES_URL в .env

```
POSTGRES_URL=postgresql://username:password@localhost:5432/rentcar_dev
```

### 4. Запустите приложение

```bash
npm run dev
```

Таблицы создадутся автоматически.

## Миграция данных из SQLite

Если у вас есть данные в SQLite и вы хотите перенести их в PostgreSQL:

### Вариант 1: Экспорт/Импорт через CSV

1. **Экспорт из SQLite:**
```bash
sqlite3 server/database/rentcar.db
.headers on
.mode csv
.output customers.csv
SELECT * FROM customers;
.quit
```

2. **Импорт в PostgreSQL:**
Используйте Vercel Dashboard → Data → Import

### Вариант 2: Использование инструментов

- **pgloader** - автоматическая миграция
- **SQLite to PostgreSQL converter** - онлайн инструменты

## Мониторинг и обслуживание

### Просмотр логов

В Vercel Dashboard:
1. Deployments → Latest Deployment
2. Functions → Logs
3. Проверяйте ошибки подключения к БД

### Мониторинг использования

Storage → ваша БД → Usage:
- Количество запросов
- Размер базы данных
- Active connections

### Бэкапы

Vercel Postgres автоматически создает бэкапы:
- Daily backups (7 дней)
- Point-in-time recovery

## Лимиты Free Plan

Vercel Postgres Free Plan включает:
- **256 MB** хранилище
- **60 hours** compute time
- **256 MB** RAM
- **Unlimited** API requests

Для production рекомендуется **Pro Plan** ($20/месяц):
- **512 MB** хранилище
- **100 hours** compute time
- **1 GB** RAM

## Оптимизация производительности

### 1. Используйте индексы

Уже настроены в [db-postgres.js](server/database/db-postgres.js):
```sql
CREATE INDEX idx_bookings_customer ON bookings(customer_id);
CREATE INDEX idx_bookings_vehicle ON bookings(vehicle_id);
CREATE INDEX idx_vehicles_status ON vehicles(status);
```

### 2. Connection Pooling

Используйте `POSTGRES_URL` (pooled connection) для большинства запросов.
Используйте `POSTGRES_URL_NON_POOLING` только для миграций.

### 3. Query optimization

- Избегайте `SELECT *` - выбирайте только нужные колонки
- Используйте `LIMIT` для больших выборок
- Добавляйте `WHERE` условия для фильтрации

## Troubleshooting

### Ошибка: "Cannot connect to database"

**Решение:**
1. Проверьте что `POSTGRES_URL` установлена в Environment Variables
2. Убедитесь что база данных подключена к проекту
3. Проверьте логи в Vercel Dashboard

### Ошибка: "relation does not exist"

**Решение:**
Таблицы не созданы. Проверьте логи инициализации БД.

Запустите вручную через Query tab в Vercel Storage.

### Ошибка: "too many connections"

**Решение:**
1. Используйте pooled connection (`POSTGRES_URL`)
2. Убедитесь что connection pool правильно настроен
3. Увеличьте plan если нужно больше connections

### Ошибка: "password authentication failed"

**Решение:**
1. Vercel автоматически устанавливает правильные credentials
2. Не изменяйте `POSTGRES_PASSWORD` вручную
3. Пересоздайте connection в Storage settings

## Безопасность

### Best Practices

1. **Никогда не коммитьте .env файлы**
   - Уже добавлено в [.gitignore](.gitignore)

2. **Используйте сильные пароли**
   - Vercel генерирует безопасные пароли автоматически

3. **Ограничьте доступ**
   - Используйте authentication middleware
   - Реализуйте role-based access control

4. **SQL Injection защита**
   - Всегда используйте параметризованные запросы
   - Уже реализовано в [query-helper.js](server/database/query-helper.js)

5. **Регулярно обновляйте зависимости**
```bash
npm audit fix
```

## Полезные ссылки

- [Vercel Postgres Documentation](https://vercel.com/docs/storage/vercel-postgres)
- [PostgreSQL Documentation](https://www.postgresql.org/docs/)
- [Node.js pg Library](https://node-postgres.com/)
- [@vercel/postgres SDK](https://vercel.com/docs/storage/vercel-postgres/sdk)

## Поддержка

Если возникли проблемы:
1. Проверьте [DEPLOYMENT.md](DEPLOYMENT.md) для общих инструкций
2. Посмотрите логи в Vercel Dashboard
3. Проверьте Vercel Status Page: https://vercel-status.com/

## Следующие шаги

После успешной настройки:
1. ✅ Создайте admin пользователя
2. ✅ Добавьте тестовые данные
3. ✅ Настройте регулярные бэкапы
4. ✅ Настройте мониторинг
5. ✅ Протестируйте все функции

Готово! Ваша CRM система теперь работает с Vercel Postgres! 🎉
